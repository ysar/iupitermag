name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    name: Lint and Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          # Keep caching on; uv will manage its cache directory.
          cache: true

      - name: Set up Python
        run: cd iupitermag-py && uv python install ${{ matrix.python-version }}

      - name: Create a virtual environment
        run: cd iupitermag-py && uv venv

      - name: Install maturin
        run: cd iupitermag-py && uv pip install maturin

      - name: Build and install Python extension
        run: cd iupitermag-py && maturin develop

      - name: Sync dependencies (incl. dev)
        run: cd iupitermag-py && uv sync --dev

      - name: Lint (ruff)
        run: cd iupitermag-py && uv run ruff check .

      - name: Format check (ruff)
        run: cd iupitermag-py && uv run ruff format --check .

      - name: Type check (pyright)
        run: cd iupitermag-py && uv run pyright

      - name: Run tests (pytest)
        run: cd iupitermag-py && uv run pytest -q

      - name: Run Rust core tests
        run: cargo test -p iupitermag-core
